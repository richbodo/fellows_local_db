First we make a simple desktop local-only db browser
Then we make a PWA that runs on mobile (at least on my android phone)
Then we implement window.ai for natural language search
Then we make it pretty.
