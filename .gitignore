# Build output
app/fellows.db
app/fellows.db.backup.*

# Python
__pycache__/
*.py[cod]
.venv/
venv/
env/

# Playwright
playwright-report/
test-results/
.playwright/
tests/e2e/screenshots/

# OS
.DS_Store

.aider*
.server.pid

# Ansible local inventory (copy from inventory/hosts.ini.example)
ansible/inventory/hosts.ini

# Production build output (rsync / Ansible target)
deploy/dist/

# Ansible Galaxy collections (see ansible/collections/requirements.yml)
ansible/collections/ansible_collections/

# Ansible run log (ansible.cfg log_path)
ansible/ansible.log

# Fellow PII — never commit. Contains contact emails, phone numbers,
# photos, and free-text fields. See README "Data Note" for how to obtain
# the source JSON and images for local development.
final_fellows_set/

# Local-only fellows data snapshots (see backup/README.md)
backup/*.zip

# Ansible become-password temp files — never commit
BECOME_PASS*
