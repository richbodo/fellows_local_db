---
# Install into this repo (from project root):
#   ansible-galaxy collection install -r ansible/collections/requirements.yml -p ansible/collections
collections:
  - name: ansible.posix
    version: ">=1.5.0"
